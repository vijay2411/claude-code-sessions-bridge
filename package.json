{
  "name": "cc-bridge",
  "version": "2.2.0",
  "description": "Real-time Q&A between Claude sessions — CLI, Desktop app, and Cowork",
  "main": "bridge-server.mjs",
  "type": "module",
  "scripts": {
    "start": "node bridge-server.mjs",
    "test": "./tests/run-all.sh",
    "install-bridge": "./install.sh",
    "uninstall-bridge": "./install.sh --uninstall",
    "check": "./install.sh --check",
    "bridge:start": "./install.sh --start",
    "bridge:stop": "./install.sh --stop",
    "bridge:restart": "./install.sh --restart"
  },
  "keywords": [
    "claude-code",
    "mcp",
    "bridge",
    "multi-session",
    "inter-session",
    "sse",
    "stdio",
    "claude-desktop"
  ],
  "author": "Vedant Vijay <vedant@mugyen.com>",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
